# Python

python script to check multiple integration between cpp and python like zmq, protobuf and msgpack

## msgpack

| file  | description  |
|---|---|
| sub_msg.py  | subscribe to message that publish by pub_msg.cpp  |
| pub_msg.py  | publish message using zmq and and serial using msgpack, using dataclass as object |